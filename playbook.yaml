# На какой группе серверов
- hosts: webservers

  tasks:
    - name: install redis server
      ansible.builtin.apt:
        name: git
        state: present
        update_cache: yes
#      become_user: user
      become: yes # <---

#    - name: remove redis server
#      ansible.builtin.apt:
#        name: redis-server
#        state: absent
#        state: present
#      become: yes # <---
